#Enable mause
setw -g mouse on

#Tiempo de escape (para vim)
set -s escape-time 0

#Sesion dev
new-session -s dev -n ide
split -v -p 10 -t dev:0
select-pane -t 1
split-h -p 10 -t dev
select-pane -t 0
